# Groq (OpenAI-compatible Chat Completions — https://console.groq.com/keys)
# Dosya kökü: web/ klasöründe (.env.local veya buradaki örnek kopyası .env)
GROQ_API_KEY=

# Optional override (default: llama-3.3-70b-versatile). See Groq model list.
# GROQ_MODEL=llama-3.3-70b-versatile
#
# Doğrulama (sunucuya anahtarı vermez): GET http://localhost:3000/api/brief → groqConfigured
